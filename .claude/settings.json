{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath // empty' | { read -r f; case \"$(basename \"$f\" 2>/dev/null)\" in HANDOFF*.md) echo '{\"systemMessage\": \"Handoff editado - toda pendencia nova documentada aqui precisa virar Issue no GitHub antes de fechar o trabalho (convencao DMMofServices, CLAUDE.md [UNIVERSAL]). Ja criou a Issue correspondente, com label e vinculada ao Project certo?\"}';; esac; }"
          }
        ]
      }
    ]
  }
}
